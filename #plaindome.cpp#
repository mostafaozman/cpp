#include <iostream>
#include <cstring> 

using namespace std; 

int main() 
{
  char input[80]; 
  char newInput[80]; 

  cout << "This is a palindrome checker, please enter a plaindrome. "
  cin.get(input, 80)
  
    for (int i = 0; i <= 80; i++)
      {
	if ( isalunm(input[i]) == false)
	  {
	    i++; 
	  }
	else
	  {
	    newInput += input[i]; 
	    i++; 
	  }
      }
 
  cout << newInput << endl; 
  
  return 0; 
}
